import React from 'react'

export default function SectionBreak({ type, outerConfig, innerConfig }) {

  let el;
  switch (type) {
    case 'parallax':

      break;
    case 'casual':
      break;
    default:
      break;
  }

  return (
    <div>
      {el}
    </div>
  )
}
